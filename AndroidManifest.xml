<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ngo2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NGO2"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UserDashboard"
            android:label="@string/title_activity_userdashboard"></activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ui.dashboard.IssuedBookDetails"/>
        <activity android:name=".ui.catalogue.BooksDetails"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>